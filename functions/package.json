{
  "name": "offboardset-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for the OffboardSet landing page",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.5.0",
    "nodemailer": "^7.0.9"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.4",
    "typescript": "^5.9.3"
  }
}
